# Exclude the Stockfish binaries/folder from language statistics
/stockfish/ linguist-vendored

# Ensure Node.js and React files are correctly identified
# (Linguist is usually good with these, but explicit can help)
*.js linguist-language=JavaScript
*.jsx linguist-language=JavaScript
*.json linguist-language=JSON
*.css linguist-language=CSS

# Exclude entire node_modules directory if it's causing issues (Linguist usually handles this by default but good to be explicit)
/node_modules/ linguist-vendored

# Exclude any other potential build artifacts or irrelevant files
# (Add patterns for any other folders/files that are misidentified)
/build/ linguist-generated
/dist/ linguist-generated
